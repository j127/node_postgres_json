import { getAll, get, set } from "./queries";
import { createMessage, createMessages } from "./generate";

const messages = {
    get,
    getAll,
    set,
    createMessage,
    createMessages,
};

export default messages;
